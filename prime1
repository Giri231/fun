#include<iostream>


using namespace std;

string prime(int a)
{
    for(int i=2;i<a;i++)
    {
        if(a%i==0)
        {
            return "not";
        }
        else
        return "prime";
    }
}

int main()
{
    int a;
    cin>>a;
    cout<<prime(a);
    return 0;
    
}
