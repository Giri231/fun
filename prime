#include<iostream>

using namespace std;

string prime(int a)
{
    if(a%2==0 || a%3==0 || a%5==0)
    {
        return "prime number";
    }
    else
    return "not";
}
int main()
{
    int a;
    cin>>a;
   cout<< prime(a);
    return 0;
    
}
