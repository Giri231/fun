#include<iostream>

using namespace std;

string marks(int a)
{
    if(a>=91 && a<=100)
    return "AA";
    else if(a>=81 && a<=90)
    return "AB";
    else if(a>=71 && a<=80)
    return "BB";
}
int main()
{
    int a;
    cin>>a;
    cout<<marks(a);
    return 0;
}
